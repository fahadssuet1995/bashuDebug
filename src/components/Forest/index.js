import React from 'react'
import VillageMainContainer from './Villages/MainContainer'



const Villages = ({ stick, user, index }) => (

  <VillageMainContainer stick={stick} user={user} index={index} />
)

export default Villages
